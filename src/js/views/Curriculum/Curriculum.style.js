import styled from "styled-components";

const CurriculumSection = styled.section`
  min-height: 100svh;
  padding: 1rem;
  padding-top: 4rem;
  display: grid;

  > div {
    max-width: 1200px;
    justify-self: center;
  }
`;

export {
  CurriculumSection
}